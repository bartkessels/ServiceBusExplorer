namespace ServiceBusExplorer.ServiceBus.Helpers
{
    using System.Collections;

    public class ReceivedServiceBusMessageExtensions
    {
        // public static IEnumerable<BrokeredMessage>   
                        // var enumerable = messages.Select(m => new BrokeredMessage(m.GetBodyStream())).ToList();
    }
}
